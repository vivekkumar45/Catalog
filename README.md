# Polynomial Solver

This project implements a simplified version of Shamir's Secret Sharing algorithm to decode and find the constant term of a polynomial based on given roots.

## Prerequisites

- Java Development Kit (JDK) 8 or higher
- JSON library (e.g., `org.json`) 

